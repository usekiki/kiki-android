<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.kiki.samples.mix">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KikiSamples">
        <activity
            android:name=".MultiFlavorSampleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- You can set your app token -->
        <meta-data
            android:name="dev.kiki.APP_TOKEN"
            android:value="ba1b2724-b476-4795-b964-eebaf6bb082d" />
    </application>


</manifest>